server.port=8083

# ── OAuth2 (old security.oauth2.* namespace used by Spring Security OAuth2 resource server) ──
security.oauth2.resource.token-info-uri=http://Micro-Auth-Service:9191/auth-api/oauth/check_token
security.oauth2.client.client-id=vst-dms-view-client
security.oauth2.client.client-secret=cnhindglobal

# ── Database - Main ──────────────────────────────────────────────────────────
jdbc.url=${DB_URL}
jdbc.username=${DB_USERNAME}
jdbc.password=${DB_PASSWORD}

# ── Database - Reports ───────────────────────────────────────────────────────
jdbc.urlForReports=${REPORT_DB_URL}
jdbc.usernameForReports=${REPORT_DB_USERNAME}
jdbc.passwordForReports=${REPORT_DB_PASSWORD}
