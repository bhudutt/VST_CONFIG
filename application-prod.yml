spring:
  datasource:
    url: jdbc:sqlserver://192.9.200.52:1433;databaseName=VST_DMS;trustServerCertificate=true
    username: sa
    password: 'VST.2022#'
  security:
    oauth2:
      resource:
        token-info-uri: http://Micro-Auth-Service:9191/auth-api/oauth/check_token
      client:
        client-id: vst-dms-view-client
        client-secret: cnhindglobal
  rabbitmq:
    host: localhost       # TODO: update with actual prod RabbitMQ host
    port: 5672
    username: guest
    password: guest
    publisher-confirm-type: correlated   # replaces deprecated publisher-confirms: true
    publisher-returns: true
    listener:
      simple:
        acknowledge-mode: manual
        prefetch: 100
  mail:
    host: 192.9.200.204
    port: 587
    username: itsupport@vsttillers.com
    password: vst.2017
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

eureka:
  client:
    service-url:
      defaultZone: http://Micro-Eureka-Service:8761/eureka/

file:
  upload-dir:
    Temp: /var/VST-DMS-APPS/FILES/Temp/
    Main: /var/VST-DMS-APPS/FILES/
