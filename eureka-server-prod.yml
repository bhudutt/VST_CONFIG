# Copy to VST_CONFIG - prod profile (vstdms.vstshakti.in)
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SELF_URL:https://vstdms.vstshakti.in/eureka/}
  server:
    enable-self-preservation: true

logging:
  level:
    root: WARN
    com.hitech: INFO
